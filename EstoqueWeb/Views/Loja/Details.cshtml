@model EstoqueWeb.Models.Loja

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes - Sistema de Controle de Estoque</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="~/css/site.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            @Html.Partial("_Layout")

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">Detalhes do Loja</h1>
                    <div class="btn-group">
                    </div>
                </div>

                <!-- Detalhes do Loja -->
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-box"></i> Informações da Loja
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="LojaValor" class="form-label">ID da Loja</label>
                                    <input type="text" class="form-control" id="LojaId" value="@Model.Id" readonly>
                                </div>
                                <div class="mb-3">
                                    <label for="LojaValor" class="form-label">Nome da Loja</label>
                                    <input type="text" class="form-control" id="LojaNome" value="@Model.Nome" readonly>
                                </div>
                                <div class="mb-3">
                                    <label for="LojaValor" class="form-label">Endereço da Loja</label>
                                    <input type="text" class="form-control" id="LojaNome" value="@Model.Endereco" readonly>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="scripts/api.js"></script>
    <script src="scripts/detalhes.js"></script>
</body>
</html>
